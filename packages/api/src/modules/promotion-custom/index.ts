// Note: This module approach may not work directly for replacing core modules
// Instead, use the service wrapper in your workflow step or register it manually
// See README.md for alternative approaches

export { default as CustomPromotionModuleService } from "./service"

