export enum BankAccountType {
  BANK = "bank",
  UPI = "upi",
}

export enum BankAccountVerificationStatus {
  CREATED = "created",
  COMPLETED = "completed",
  FAILED = "failed",
}

export enum RazorpayValidationType {
  PENNYDROP = "pennydrop",
  PENNYILESS = "pennyiless",
  OPTIMIZED = "optimized",
}

export enum CustomerUpiDetailStatus {
  ACTIVE = "active",
  INACTIVE = "inactive",
}

export enum CustomerBankDetailStatus {
  ACTIVE = "active",
  INACTIVE = "inactive",
}
